<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:gravity="center"
    android:orientation="vertical"
    android:padding="16dp">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Please pick your preferred option"
        android:textSize="24sp"/>

    <Button
        android:id="@+id/student_register_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Student Register"
        android:onClick="onStudentRegisterButtonClicked" />

    <Button
        android:id="@+id/lecturer_register_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Lecturer Register"
        android:onClick="onLecturerRegisterButtonClicked" />

</LinearLayout>
